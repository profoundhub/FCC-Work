function bouncer(arr) {
  // Don't show a false ID to this bouncer.
  
  var filteredArray = arr.filter(Boolean);
  return filteredArray;
  
//  var iArray;
  
//  function filterArray(obj) {
//  if (arr === null || arr === "0" || arr === undefined || arr === "NaN" ) {
//    return true;
//  } else {
//    iArray++;
//    return false;
//  }
//}
  
//  var arrModify = arr.filter(filterArray);
//  return arr;
}

bouncer([7, "ate", "", false, 9]);
